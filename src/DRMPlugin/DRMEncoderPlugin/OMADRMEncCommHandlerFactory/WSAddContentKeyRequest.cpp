/*
 * Copyright 2003-2004 The Apache Software Foundation.

 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *		http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * This file was auto-generated by the Axis C++ Web Service Generator (WSDL2Ws)
 * This file contains functions to manipulate complex type WSAddContentKeyRequest
 */

#include "WSAddContentKeyRequest.hpp"
#include <axis/AxisWrapperAPI.hpp>

/*
 * This static method serialize a WSAddContentKeyRequest type of object
 */
int Axis_Serialize_WSAddContentKeyRequest(WSAddContentKeyRequest* param, IWrapperSoapSerializer* pSZ, bool bArray = false)
{
	if (bArray)
	{
		pSZ->serialize("<", Axis_TypeName_WSAddContentKeyRequest, ">", NULL);
	}
	else
	{
		bool blnIsNewPrefix = false;
		const AxisChar* sPrefix = pSZ->getNamespacePrefix(Axis_URI_WSAddContentKeyRequest, blnIsNewPrefix);
		if (!blnIsNewPrefix)
		{
			pSZ->serialize("<", Axis_TypeName_WSAddContentKeyRequest, " xsi:type=\"", sPrefix, ":",
				Axis_TypeName_WSAddContentKeyRequest, "\">", NULL);
		}
		else
		{
			pSZ->serialize("<", Axis_TypeName_WSAddContentKeyRequest, " xsi:type=\"", sPrefix, ":",
				Axis_TypeName_WSAddContentKeyRequest, "\" xmlns:", sPrefix, "=\"",
				Axis_URI_WSAddContentKeyRequest, "\">", NULL);
		}
	}

	pSZ->serializeAsElement("msg", (void*)(param->msg), XSD_STRING);

	pSZ->serialize("</", Axis_TypeName_WSAddContentKeyRequest, ">", NULL);
	return AXIS_SUCCESS;
}

/*
 * This static method deserialize a WSAddContentKeyRequest type of object
 */
int Axis_DeSerialize_WSAddContentKeyRequest(WSAddContentKeyRequest* param, IWrapperSoapDeSerializer* pIWSDZ)
{
	param->msg = pIWSDZ->getElementAsString("msg",0);
	return pIWSDZ->getStatus();
}
void* Axis_Create_WSAddContentKeyRequest(WSAddContentKeyRequest* pObj, bool bArray = false, int nSize=0)
{
	if (bArray && (nSize > 0))
	{
		if (pObj)
		{
			WSAddContentKeyRequest* pNew = new WSAddContentKeyRequest[nSize];
			memcpy(pNew, pObj, sizeof(WSAddContentKeyRequest)*nSize/2);
			memset(pObj, 0, sizeof(WSAddContentKeyRequest)*nSize/2);
			delete [] pObj;
			return pNew;
		}
		else
		{
			return new WSAddContentKeyRequest[nSize];
		}
	}
	else
		return new WSAddContentKeyRequest;
}

/*
 * This static method delete a WSAddContentKeyRequest type of object
 */
void Axis_Delete_WSAddContentKeyRequest(WSAddContentKeyRequest* param, bool bArray = false, int nSize=0)
{
	if (bArray)
	{
		delete [] param;
	}
	else
	{
		delete param;
	}
}
/*
 * This static method gives the size of WSAddContentKeyRequest type of object
 */
int Axis_GetSize_WSAddContentKeyRequest()
{
	return sizeof(WSAddContentKeyRequest);
}

WSAddContentKeyRequest::WSAddContentKeyRequest()
{
	/*do not allocate memory to any pointer members here
	 because deserializer will allocate memory anyway. */
	memset( &msg, 0, sizeof( xsd__string));
}

WSAddContentKeyRequest::~WSAddContentKeyRequest()
{
	/*delete any pointer and array members here*/
}
