/*
 * Copyright 2003-2004 The Apache Software Foundation.

 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *		http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * This file was auto-generated by the Axis C++ Web Service Generator (WSDL2Ws)
 * This file contains functions to manipulate complex type OsmsCreateUserFault
 */

#include "OsmsCreateUserFault.hpp"
#include <axis/AxisWrapperAPI.hpp>

/*
 * This static method serialize a OsmsCreateUserFault type of object
 */
int Axis_Serialize_OsmsCreateUserFault(OsmsCreateUserFault* param, IWrapperSoapSerializer* pSZ, bool bArray = false)
{
	if (bArray)
	{
		pSZ->serialize("<", Axis_TypeName_OsmsCreateUserFault, ">", NULL);
	}
	else
	{
		bool blnIsNewPrefix = false;
		const AxisChar* sPrefix = pSZ->getNamespacePrefix(Axis_URI_OsmsCreateUserFault, blnIsNewPrefix);
		if (!blnIsNewPrefix)
		{
			pSZ->serialize("<", Axis_TypeName_OsmsCreateUserFault, " xsi:type=\"", sPrefix, ":",
				Axis_TypeName_OsmsCreateUserFault, "\">", NULL);
		}
		else
		{
			pSZ->serialize("<", Axis_TypeName_OsmsCreateUserFault, " xsi:type=\"", sPrefix, ":",
				Axis_TypeName_OsmsCreateUserFault, "\" xmlns:", sPrefix, "=\"",
				Axis_URI_OsmsCreateUserFault, "\">", NULL);
		}
	}

	pSZ->serializeAsElement("errorCode", (void*)&(param->errorCode), XSD_INT);
	pSZ->serializeAsElement("responseString", (void*)(param->responseString), XSD_STRING);

	pSZ->serialize("</", Axis_TypeName_OsmsCreateUserFault, ">", NULL);
	return AXIS_SUCCESS;
}

/*
 * This static method deserialize a OsmsCreateUserFault type of object
 */
int Axis_DeSerialize_OsmsCreateUserFault(OsmsCreateUserFault* param, IWrapperSoapDeSerializer* pIWSDZ)
{
	xsd__int* p_errorCode = (pIWSDZ->getElementAsInt("errorCode",0));
	param->errorCode = *p_errorCode;
	delete p_errorCode;
	param->responseString = pIWSDZ->getElementAsString("responseString",0);
	return pIWSDZ->getStatus();
}
void* Axis_Create_OsmsCreateUserFault(OsmsCreateUserFault* pObj, bool bArray = false, int nSize=0)
{
	if (bArray && (nSize > 0))
	{
		if (pObj)
		{
			OsmsCreateUserFault* pNew = new OsmsCreateUserFault[nSize];
			memcpy(pNew, pObj, sizeof(OsmsCreateUserFault)*nSize/2);
			memset(pObj, 0, sizeof(OsmsCreateUserFault)*nSize/2);
			delete [] pObj;
			return pNew;
		}
		else
		{
			return new OsmsCreateUserFault[nSize];
		}
	}
	else
		return new OsmsCreateUserFault;
}

/*
 * This static method delete a OsmsCreateUserFault type of object
 */
void Axis_Delete_OsmsCreateUserFault(OsmsCreateUserFault* param, bool bArray = false, int nSize=0)
{
	if (bArray)
	{
		delete [] param;
	}
	else
	{
		delete param;
	}
}
/*
 * This static method gives the size of OsmsCreateUserFault type of object
 */
int Axis_GetSize_OsmsCreateUserFault()
{
	return sizeof(OsmsCreateUserFault);
}

OsmsCreateUserFault::OsmsCreateUserFault()
{
	/*do not allocate memory to any pointer members here
	 because deserializer will allocate memory anyway. */
	memset( &errorCode, 0, sizeof( xsd__int));
	memset( &responseString, 0, sizeof( xsd__string));
}

OsmsCreateUserFault::~OsmsCreateUserFault() throw ()
{
	/*delete any pointer and array members here*/
}
